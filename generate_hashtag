def generate_hashtag(s):
    # get words
    words = s.split()
	    
    # verify False conditions
    if not words or len(s) > 140:
        return False

    # make hashtag
    words = [word.capitalize() for word in words]
    output = '#'+''.join(words)
    return output
